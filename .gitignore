__pycache__/
*.pyc
.pytest_cache/
.venv/
*.egg-info/

# MIMIC-IV demo download (regenerated by `make download-mimic`)
data/mimic-iv-demo/
data/mimic-iv-demo.zip

# Local memory store (per-run, not tracked)
data/memory.sqlite3

# Generated outputs
docs/e2e/index.html
docs/validation/reliability_diagram.png

# OS / IDE
.DS_Store
Thumbs.db
.idea/
.vscode/
.cadence/

# Claude Code session context (project-private)
CLAUDE.md
.claude/

# Coverage runtime
.coverage
htmlcov/

# Local env
.env

# OAuth client registry (contains client_secret_hash + JWT signing secret in .env)
data/oauth_clients.json

# Caddy runtime / logs
logs/
caddy_data/

# Background stack control PID files
.stack_pids/

# Reproducibility / audit runtime DBs (generated on every run)
data/reproducibility_archive.sqlite3
data/audit.jsonl

# FHIR Bundle fixtures (~94 MB, fetched via `make download-fixtures`)
fixtures/*.json

# Internal R&D — calibration workflow design + presets, NOT part of deliverable
TRUSTEDRISK_DESIGN.md
DESIGN_DOC_PATCHES_v0.3.md
OUTPUT_SCHEMA_*.md
preset_rewrite_guide.md
doubts.md
presets/
audit_outputs/
docs/WORKFLOWS_OUTCOMES.md

# Session-scoped pending design log + one-shot helpers (kept locally,
# not part of the deliverable)
DESIGN_QUESTIONS_PENDING.md
scripts/_marcus_soap_gen.py

